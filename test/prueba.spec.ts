describe('prueba.ts', () => {
  it('prueba 01', () => {
    // Preparación
    const number01 = 10;
    const number02 = 20;

    // Ejecución
    const sum = number01 + number02;

    // Comprobación
    expect(sum).toBe(30);
  });
});
